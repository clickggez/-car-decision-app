# CarDSS — คำสั่งสำหรับ Claude Code

## 🛑 อ่านก่อนทำอะไร

อ่าน [`AGENTS.md`](AGENTS.md) แล้วทำตามที่มันบอก (มันจะพาไป `agent-docs/00-READ-FIRST.md` ต่อ)
ข้อห้ามและกฎการแบ่งงานอยู่ใน [`.cursorrules`](.cursorrules)

**สื่อสารกับผู้ใช้เป็นภาษาไทยเสมอ** ผู้ใช้อ่านภาษาอังกฤษไม่ออก — ต้องแปล error เป็นไทยให้ด้วย

---

## 🗣️ ห้องประชุม AI

โปรเจกต์นี้มี **ห้องประชุมกลาง** ให้ AI 3 ตัว (Claude / Codex / Antigravity) คุยกันได้จริง
ข้อความเก็บถาวรที่ `agent-docs/meeting/board.json` และติดตามใน git — **ไม่หายเมื่อเปิดเซสชันใหม่**

วิธีใช้ทั้งหมดอยู่ใน [`agent-docs/meeting/README.md`](agent-docs/meeting/README.md)

**เริ่มเร็ว:** พิมพ์ `/meeting` หรืออ่านเองด้วย
```bash
node agent-docs/meeting/server.mjs read      # อ่านทั้งหมด
node agent-docs/meeting/server.mjs topics    # ดูหัวข้อ
```

### เรียก AI ตัวอื่นมาร่วมประชุมได้จากเซสชันนี้เลย

**Antigravity** — โพสต์เข้าห้องประชุมเองได้ (สิทธิ์ตั้งไว้แล้ว: อ่านไฟล์ได้ เขียนไม่ได้ รัน shell ไม่ได้)
```bash
"C:/Users/click/AppData/Local/agy/bin/agy.exe" -p "คำสั่ง"
```

**Codex** — เรียก MCP ไม่ได้ ต้องรับคำตอบมาแล้ว Claude โพสต์แทน (ระบุกำกับด้วยว่าส่งแทน)
```bash
find ~/AppData/Local/OpenAI -name codex.exe   # หา path (มี hash เปลี่ยนตามเวอร์ชัน)
"<path>/codex.exe" exec --skip-git-repo-check "คำสั่ง"
```

### กติกาเวลาจัดประชุม

1. **ตั้งคำถามที่บังคับให้เปิดไฟล์จริง** ไม่งั้นได้แต่คำโฆษณา — สั่งให้ระบุไฟล์และบรรทัดกำกับทุกข้อ
2. **ตรวจทุกข้ออ้างก่อนเชื่อ** เปิดไฟล์ยืนยันเอง แล้วโพสต์ผลตรวจลงกระดาน
3. **อย่านับคำชมระหว่าง AI เป็นหลักฐาน** ทั้งคู่ถูกเรียกจากเซสชันนี้ อาจเกรงใจ ให้ดูจากของที่หาเจอจริง
4. ข้อสรุปสุดท้าย **ผู้ใช้เป็นคนเคาะ** แล้วบันทึกลง `agent-docs/04-handoff.md`

---

## Subagent ประจำโปรเจกต์ (`.claude/agents/`)

- `cardss-guard` — ตรวจว่าแผน/โค้ดผิดข้อห้ามไหม (อ่านอย่างเดียว)
- `cardss-numbers` — ตรวจตัวเลขว่าตรงไฟล์ดิบใน `analysis/` ไหม (อ่านอย่างเดียว)
- `cardss-handoff` — ปิดงานตามโปรโตคอล (แก้ได้เฉพาะ `agent-docs/`)
