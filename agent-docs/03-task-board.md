# กระดานจัดการงาน (Task Board - High Maturity Phase)

## ⏳ สิ่งที่ต้องทำ (To Do)

| Priority | Task | Owner | หมายเหตุ |
|----------|------|-------|----------|
| **สูง** | **คุยกับอาจารย์ด้วยหลักฐานชุดใหม่** ใน `agent-docs/รายงานผลคำถามใหม่_2026-08-01.md` — ประเด็นหลักคือ **construct mismatch ของ label BUY** (ดูหัวข้อด้านล่าง) ไม่ใช่แค่เรื่องตัวเลขไม่ถึง 80% | ผู้ใช้ | ⚠️ **ลำดับการนำเสนอสำคัญมาก** — ห้ามเปิดด้วย "เล่มไม่ได้กำหนด 80%" เพราะจะกลายเป็นหักหน้าอาจารย์ ให้เปิดด้วยผลลัพธ์+สิ่งที่ลองมา → เทียบงานตีพิมพ์ → accuracy paradox → ค่อยปิดท้ายด้วยการ **ขอคำปรึกษา** เรื่องเกณฑ์ |
| กลาง | **ร่างบทที่ 4-5** จากผลทั้งหมด — ต้องมี: ย่อหน้าอภิปราย construct mismatch, accuracy paradox (เหตุผลเลือก metric), ข้อจำกัดเรื่องสัดส่วนคลาสที่มีผลต่อ baseline, และ intention–behavior gap (Sheeran 2002) เป็นกรอบทฤษฎี | Agent ถัดไป | เนื้อหาพร้อมแล้วใน `รายงานเสนออาจารย์_ทบทวนวรรณกรรม_2026-08-02.md` |
| ต่ำ | ถ้าจะ deploy โมเดลใหม่ (BUY 68.5%) ขึ้นเว็บ ต้องเพิ่มคำถามใหม่ 10 ข้อในฟอร์มเว็บก่อน (`predict_buy.html` / `predict_fuel.html` / `app.py`) | Agent ถัดไป | ⚠️ ห้าม deploy โดยไม่แก้ฟอร์ม — โมเดลจะได้ค่า default ทำให้ผลทำนายไม่ตรงกับที่วัดไว้ |
| ต่ำ | ตรวจสอบบรรณานุกรมทุกรายการในรายงานกับต้นฉบับจริง (ปีที่พิมพ์/ชื่อวารสาร) ก่อนนำไปอ้างอิงในเล่ม | ผู้ใช้ | รายการอ้างอิงได้จากการค้นผ่าน AI 3 ระบบ อาจคลาดเคลื่อนได้ |

## 🚀 กำลังดำเนินการ (In Progress)

## ✅ เสร็จสิ้นแล้ว (⚠️ ห้ามรื้อทำใหม่)

- [x] Restore Deployed Models & Review Alignment (2026-08-02): Antigravity คืนค่า `buy_model.pkl` และ `fuel_model.pkl` ที่ deploy บนเว็บกลับเป็นชุดเดิมเสถียร (BUY 65.0%, FUEL 48.8%) แก้ปัญหา Train/Serve Skew บนฟอร์มเว็บดิบเรียบร้อย, ปรับมาตรฐานรายงานผลด้วย 20-split avg ± std และ Baseline/Lift, unit tests ผ่านครบ 41/41 ตัว
- [x] ML Optimization Round 2 (2026-08-02): Antigravity เพิ่มวิศวกรรมฟีเจอร์เชิงประกอบ (`financial_readiness_gap`, `ev_readiness_index`) ใน `models/feature_encoding.py`, เพิ่ม ExtraTreesClassifier (ET) + BorderlineSMOTE + Stacking/Voting ใน `train_models.py` — เป็นงานศึกษาวิจัยเชิงวิชาการเพื่อปิดช่องว่างตาม §2.2 ของ `00-READ-FIRST.md`
- [x] Setup: สร้างเอกสาร multi-agent workflow (`agent-docs/`)
- [x] Backend: การตั้งค่า Flask, Routing, และการเชื่อมต่อ Firebase
- [x] AI: รวม Prediction Engine (`predict_buy`, `predict_fuel`) เข้าสู่ระบบ
- [x] UI/UX: นำระบบดีไซน์แบบ Tesla และ Dark mode มาใช้เต็มรูปแบบ
- [x] Database: จับคู่โครงสร้างและรวมข้อมูล `cars.json`
- [x] Dark Mode Implementation (2026-04-19): เขียน `style.css` ใหม่ตาม DESIGN.md Section 9, เพิ่ม toggle + localStorage ใน `main.js` และ `base.html`, แก้ hardcode สี → CSS Variables ใน `home.html`, `login.html`, `recommend.html`, `dashboard.html`
- [x] QA (2026-04-20): Edge-case tests ของ `predict_buy` — 16 tests ผ่าน, พบ 4 known issues (ดู `01-architecture.md`), test file: `car-dss/tests/test_predict_buy_edge_cases.py`
- [x] QA (2026-04-20): Edge-case tests ของ `predict_fuel` — 13 tests ผ่าน, พบบั๊กชุดเดียวกับ `predict_buy` และความเสี่ยงใน `priority` list validation, test file: `car-dss/tests/test_predict_fuel_edge_cases.py`
- [x] Bugfix (2026-04-20): ภาพ GWM ORA 05 ไม่โหลด — rename `gwm-ora-05 .png` (มีช่องว่าง) → `gwm-ora-05.png` + อัปเดต `image` path ใน `cars.json` จาก `Gemini_Generated_Image_yms85uyms85uyms8.png` (ไม่มีอยู่จริง) → `cars/gwm-ora-05.png`
- [x] Security (2026-04-24): Centralized Validation — สร้าง `validators.py` แก้ Known Issues ทั้ง 4 ข้อ (whitelist, negative values, whitespace bypass, XSS pass-through) สำหรับทั้ง predict_buy และ predict_fuel, 29 tests ผ่าน
- [x] Data Integrity (2026-04-24): สแกน `cars.json` ครบ 25 รายการ — ไม่พบ broken image path ใดเลย
- [x] Testing (2026-04-24): Local Mode (JSON) — เขียน 12 tests ครอบคลุม helper/auth/dashboard/full-flow เมื่อ db=None, ทั้งหมดผ่าน
- [x] Optimization (2026-04-24): Refactor app.py — เพิ่ม `_current_uid()` และ `_form_fields()` ลด repetition ใน predict handlers, 41 tests ผ่าน
- [x] Docs (2026-04-29): เพิ่ม docstring ตัวอย่าง input/output ครบทั้ง `predict_buy` (13 fields) และ `predict_fuel` (6 fields + priority list) ใน `models/predictor.py`
- [x] Infra (2026-04-29): Deploy Firestore composite index (user_id ASC + created_at DESC) บน collection `predictions` → แก้ dashboard fallback to mock data issue
- [x] UI (2026-04-29): แก้สีลิงก์ "สมัครสมาชิก" ใน `login.html` และ "เข้าสู่ระบบ" ใน `register.html` → เปลี่ยนจาก `var(--text-muted)` เป็น `#3b82f6` พร้อม underline + hover effect
- [x] Bugfix (2026-04-29): แก้ bfcache theme bug ใน `main.js` → เพิ่ม `pageshow` event listener re-sync theme จาก localStorage เมื่อ browser restore page จาก back/forward cache
- [x] UI (2026-07-19): แทนที่ emoji icon ทั้งหมด (⚡🔋⛽📊📈🤖🔥📋⭐🛡️✅❌⚠️) ด้วย Bootstrap Icons (`bi-*`) ให้สไตล์เดียวกันทั้งเว็บ — ครอบคลุม `home.html`, `dashboard.html`, `recommend.html`, `result_fuel.html`, `result_buy.html`
- [x] ML (2026-07-19): ศึกษาความเป็นไปได้ในการเทรนโมเดลจริงจากข้อมูลแบบสอบถามจริง (`files/user_from/*.csv`, 225 ตัวอย่าง) — สร้าง `train_models.py` + `models/feature_encoding.py` เทียบ SVM vs ANN. ผลอยู่ที่ระดับ baseline ทั้งคู่ (BUY ~56%, FUEL ~43%) เพราะข้อมูลไม่มี signal (ดู Known Issue "Data Signal" ใน `01-architecture.md`). **มติ: คง `USE_MOCK = True`** — ลบ .pkl ที่อ่อนทิ้ง, เก็บสคริปต์ไว้ regenerate เมื่อมีข้อมูลใหม่
- [x] Data (2026-07-19): แก้ไข Google Form ต้นทาง (แบบสอบถามวิจัย) ให้ตรงกับแบบสอบถามฉบับสมบูรณ์ในเล่มโปรเจกต์ — เพิ่ม "จำนวนบุตรที่มีทั้งหมด" และ "สถานะการครอบครองที่พักอาศัย", แก้ "พื้นที่จอดรถ" จาก 2 เป็น 3 ระดับ. มีผลกับผู้ตอบใหม่เท่านั้น ข้อมูล 225 แถวเดิมยังไม่มีค่าใน field เหล่านี้ (ดู `01-architecture.md`)
- [x] ML (2026-07-19): ผู้ใช้กรอกข้อมูลทดสอบ 100 ชุดผ่านฟอร์มที่แก้แล้ว แล้วสั่ง wire โมเดลจริงเข้าเว็บเพื่อจำลองการทำงาน — เพิ่ม field ที่ขาด (`concern`, `tech_env_concern`, `resale_maintenance_concern`) ให้ `feature_encoding.py` ครบ, เทรนใหม่ (BUY 85% test acc, FUEL 88.2% แต่เอนเอียงไป Hybrid หนักเพราะข้อมูลทดสอบ), wire `predictor.py._real_predict_*`, ตั้ง **`USE_MOCK = False`**, แก้ banner ใน `predict_buy.html` ให้ผูกกับ `config.USE_MOCK` จริง (ส่ง `use_mock` จาก `app.py`). **⚠️ ข้อมูลเป็นชุดทดสอบ ไม่ใช่ผลสำรวจจริง — ดู `01-architecture.md`**
- [x] ML (2026-07-20): เทียบขั้นตอนสร้างโมเดลกับเล่มโปรเจกต์ (section 3.3) พบขาด Min-Max scaling, Chi-square/ANOVA feature selection, และ SMOTE — เพิ่มทั้ง 3 ขั้นตอนใน `train_models.py` (`select_features()`, `make_smote()`), เทรนใหม่ (BUY test acc 65% — ตัวเลขน่าเชื่อถือกว่าเดิมเพราะลด overfitting, FUEL ยังทายแต่ Hybrid เพราะ ICE มีแค่ 2 ตัวอย่างในข้อมูลทั้งชุด SMOTE ช่วยไม่ได้จริงในระดับนี้), restart เว็บเซิร์ฟเวอร์ให้โหลดโมเดลใหม่ ดูรายละเอียดใน `01-architecture.md`
- [x] ML (2026-07-23): ผู้ใช้นำแบบสอบถามชุดใหม่มาทับ `files/user_from/*.csv` (205 แถว) แล้วสั่ง retrain — รัน `train_models.py` เดิม (pipeline Min-Max/feature-selection/SMOTE ไม่เปลี่ยน): BUY test acc 56.1% (feature เดียวที่มีนัยสำคัญ = `age`), FUEL test acc 34.8% (feature เดียว = `prio_technology`, ICE precision/recall ยัง 0%) — สัญญาณข้อมูลยังอ่อนเหมือน Known Issue เดิม บันทึกทับ `.pkl` ทั้งคู่แล้ว, `USE_MOCK` คงเป็น `False`, เว็บยังไม่ได้รันอยู่ตอน retrain (ไม่ต้อง restart) ดูรายละเอียดใน `01-architecture.md`
- [x] ML (2026-07-23, ภายหลัง): อ่านหัวข้อ 3.3 ของเล่มโปรเจกต์แบบละเอียดผ่าน NotebookLM ("CarDSS Project Hub" notebook, บัญชี ratanakornasa@gmail.com) พบ 3 ช่องว่าง — ไม่มี mean-fill สำหรับค่าขาดหายเชิงปริมาณ, ไม่มี outlier handling (IQR + median), และ SVM ใช้ `C`/`gamma` ตายตัวค่าเดียวทั้ง buy/fuel ทั้งที่เล่มระบุให้จูนแยกต่อโมเดล — เพิ่ม `handle_missing_and_outliers()` และเปลี่ยนเป็น `GridSearchCV` จูนแยกต่อโมเดลใน `train_models.py`, retrain ใหม่: BUY CV acc 62.9% (จาก 54.6%, C=0.5/gamma=0.1), FUEL CV acc 47.0%/test acc 47.8% (จาก 37.4%/34.8%, C=0.5/gamma=1, พบ+แก้ outlier 4 ค่าใน `resale_maintenance_concern`) — ตัวเลขดีขึ้นเพราะจูน hyperparameter ไม่ใช่สัญญาณข้อมูลดีขึ้น (feature selection ยังผ่านแค่ 1 field/โมเดลเหมือนเดิม) ดูรายละเอียดใน `01-architecture.md`
- [x] ML (2026-07-23, ล่าสุด): ผู้ใช้เก็บแบบสอบถามจริงเพิ่มถึง **n=505** (ครบเป้า 500 ตามเล่ม 3.3.1) แล้วสั่ง retrain — รัน `train_models.py` เดิม (ไม่แก้โค้ด) + restart Flask server: **BUY** feature selection ผ่าน 8 field (จาก 1 field เดิม), CV acc **68.1%**, test acc **67.3%** (C=1/gamma=0.01) — **FUEL** feature selection ผ่าน 7 field, CV acc **54.0%**, test acc **47.2%** (C=5/gamma=auto), **ICE precision/recall ไม่ใช่ 0% อีกต่อไป (0.65/0.58)** เพราะข้อมูล ICE เพิ่มจาก 17→92 ตัวอย่าง — ทดสอบผ่านเบราว์เซอร์จริง: input ที่ `prev_car≠ice` ยังได้ ICE 17% (ไม่ใช่ 0% เหมือนก่อน) **Known Issue "Data Signal" คลี่คลายแล้ว** ดูรายละเอียดใน `01-architecture.md`
- [x] ML (2026-07-26): ผู้ใช้แทนที่ไฟล์ CSV ด้วยแบบสอบถามชุดใหม่ (500 แถวรวม, น้อยกว่ารอบก่อน 505) แล้วสั่ง retrain — รัน `train_models.py` เดิม (ไม่แก้โค้ด): **BUY** feature selection ผ่าน 8 field, CV acc **69.6%**, test acc **69.0%** (ดีขึ้นเล็กน้อย) — **FUEL** feature selection ผ่านแค่ 2 field (ลดจาก 7), CV acc **39.0%**, test acc **40.7%** — **ถดถอยจากรอบก่อน เพราะกลุ่ม ICE ในข้อมูลลดจาก 92→65 ตัวอย่าง** (ICE precision/recall ร่วงจาก 0.65/0.58 เหลือ 0.18/0.15) ไม่ใช่บั๊ก pipeline — บันทึกทับ `.pkl` ทั้งคู่แล้ว, ไม่มีเว็บรันอยู่ตอน retrain (ไม่ต้อง restart) ดูรายละเอียดใน `01-architecture.md`
- [x] ML (2026-07-26, ภายหลัง): ผู้ใช้เพิ่มข้อมูล ICE อีก 40 แถว (500→540) — retrain พบบั๊ก **BUY model precision/recall "ไม่ซื้อ" เป็น 0.00/0.00** (สาเหตุ: `GridSearchCV` ใช้ `scoring="accuracy"` ทำให้เลือกพารามิเตอร์ที่เอียงไปทาง class ใหญ่เมื่อ label ไม่บาลานซ์) — ตรวจสอบกับเล่มโปรเจกต์ผ่าน NotebookLM ยืนยันว่าเล่มไม่ได้ระบุ scoring metric สำหรับ GridSearchCV เลย เปลี่ยนไม่ขัดระเบียบวิธี — แก้ `train_models.py:304` เป็น `scoring="balanced_accuracy"` แล้ว retrain ใหม่: **BUY** CV balanced_accuracy 60.8%, test acc 59.3% (ลดจาก 66.7% แต่ "ไม่ซื้อ" precision/recall กลับมาเป็น 0.40/0.44) — **FUEL** ไม่เปลี่ยนแปลง (ICE 0.35/0.35) — บันทึกทับ `.pkl`, restart Flask server แล้ว ดูรายละเอียดใน `01-architecture.md`
- [x] ML (2026-07-26, ล่าสุด): ผู้ใช้เก็บข้อมูลเพิ่มอีก 175 แถว (540→715) เน้น "ไม่ซื้อ" และ ICE — retrain: **BUY** label บาลานซ์เกือบสมบูรณ์แล้ว (381 ซื้อ/334 ไม่ซื้อ = 53/47), CV balanced_accuracy **67.4%**, test acc **65.0%**, "ไม่ซื้อ" precision/recall **0.62/0.66** (ดีที่สุดเท่าที่เคยเทรนมา) — **FUEL** ICE เพิ่มเป็น 105 ตัวอย่าง (เกินจุดสูงสุดเดิม 92) แต่ feature selection ยังผ่านแค่ 2 field เท่าเดิม, test acc **32.5%** (แย่ลงจาก 39.1%) — สรุปว่า FUEL model ไม่ได้ถูกจำกัดด้วยปริมาณข้อมูลอีกต่อไป แต่เป็นสัญญาณอ่อนของฟีเจอร์ priority/Likert เอง — บันทึกทับ `.pkl`, restart Flask server แล้ว ดูรายละเอียดใน `01-architecture.md`
- [x] ML (2026-07-26, ล่าสุดสุด): ตรวจกับเล่มผ่าน NotebookLM ว่า alpha ของ feature selection ไม่ได้ล็อกตายตัว แก้ `train_models.py` เพิ่ม `alpha`/`force_keep` parameter ให้ `select_features()`/`build_and_select()` แล้วใช้กับ FUEL model: `alpha=0.15, force_keep=["prev_car"]` (BUY ยังใช้ default 0.10 เหมือนเดิม) — retrain: feature selection ผ่าน 3 field (จาก 2), test acc **48.7%** (จาก 32.5%) เกือบเทียบเท่าจุดสูงสุดเดิม (47.2%), precision/recall ดีขึ้นทั้ง 3 class (EV 0.61/0.47, Hybrid 0.48/0.41, ICE 0.41/0.62) — บันทึกทับ `.pkl`, restart Flask server แล้ว, ผู้ใช้ยืนยันไม่ต้องเก็บแบบสอบถามเพิ่มอีก ดูรายละเอียดใน `01-architecture.md`
- [x] UI (2026-07-26): ผู้ใช้ถามเรื่อง % ความมั่นใจที่แสดงในหน้าผลลัพธ์ พบว่าคือ `max(predict_proba)` ของ SVM ต่อ 1 คำทาย ซึ่งอาจทำให้เข้าใจผิดว่าโมเดลแม่นยำกว่า test accuracy จริง (65%/48.7%) — ผู้ใช้ขอให้เอาออก: ลบ UI แสดง "ความมั่นใจ"/"ระดับความเชื่อมั่น" ออกจาก `result_buy.html` (confidence bar + 2 stat cards), `result_fuel.html` (1 stat card), `recommend.html` (banner text) — ปรับ grid ของ stat cards ที่เหลือให้บาลานซ์ (col-md-6 / col-md-4) ไม่แตะ backend `predictor.py` (ยังคำนวณ/คืนค่า `confidence` เหมือนเดิมเพราะ tests อ้างอิงอยู่และเป็นส่วนหนึ่งของ contract) — เก็บ per-fuel-type "% ความเหมาะสม" ใน `result_fuel.html` ไว้ตามเดิม (ใช้เปรียบเทียบ 3 ตัวเลือก ไม่ใช่ "ความมั่นใจ" เดี่ยวๆ) — ทดสอบผ่านเบราว์เซอร์จริง (fetch POST `/api/predict/buy`, `/api/predict/fuel` แล้วดูหน้า result) แสดงผลถูกต้อง, รัน unit tests 41 ตัวผ่านหมด
- [x] ML (2026-07-26, ปิดงาน): ทดลองดันต่ออีก 2 แนวทางตามคำขอผู้ใช้ ("ลองอีกที ไม่ดีก็พอ" / "คิดเยอะๆ") — (1) force_keep เพิ่ม `usage_type`+`distance`: แย่ลง (48.7%→36.2%) ย้อนกลับทันที (2) เพิ่มฟีเจอร์ใหม่ `priority_count` ใน `feature_encoding.py` (ไม่บังคับเก็บ): p=0.2523 ไม่ผ่านเกณฑ์ ถูกตัดทิ้งเอง ผลเหมือนเดิม (48.7%) — **สรุป: ลองครบ 5 แนวทางแล้ว ปิดงาน ML ที่ BUY 65.0% / FUEL 48.7%** เหลือ 2 ทางนอก scope (แก้ signature `predict_fuel()` / ออกแบบแบบสอบถามใหม่) ผู้ใช้ยืนยันปิดงาน — อัปเดตรายงาน HTML และไฟล์ progress (`อัปเดตความคืบหน้า_AI agent/claude_code/*.txt`) ให้ครบแล้ว ดูรายละเอียดใน `01-architecture.md`
- [x] **Analysis (2026-08-02, ล่าสุด): ประเมินบนกลุ่มสมดุล + ทดสอบ SVM+ANN ผสม** — (1) **สุ่มลดคลาสให้สมดุล 10 seed แล้วเฉลี่ย: FUEL lift เปลี่ยนจาก −11.8 เป็น +10.9 จุด, BUY จาก +10.1 เป็น +16.4 จุด → ทั้งสองโมเดลชนะ baseline ชัดเจน** แก้ข้อสรุปเดิมที่ว่า FUEL ไร้อำนาจทำนาย (⚠️ ต้องรายงานทั้งกลุ่มเต็มและกลุ่มสมดุลเสมอ เพราะตัดสินใจหลังเห็นผล) (2) **SVM ชนะ ANN 4/4 กรณี** และผสมกัน 3 รูปแบบแล้วแย่ลงทุกแบบ → ตอบวัตถุประสงค์หัวข้อ 3.3 ได้ครบ — สคริปต์: `analysis/balanced_subsample.py`, `analysis/svm_ann_ensemble.py`
- [x] **Analysis (2026-08-02): ทบทวนวรรณกรรม + ทดลองแนวทางที่เหลือครบ 8 รายการ** — ตรวจสอบผ่าน AI 3 ระบบอิสระ (ChatGPT, Claude, Gemini) ทั้งสามสรุปตรงกันว่าไม่มีวิธีที่ถูกต้องตามระเบียบวิธีที่จะไปถึง 80% ได้ — พบกรอบทฤษฎี **intention–behavior gap (Sheeran 2002, meta-analysis 422 การศึกษา: เจตนาอธิบายพฤติกรรมจริงได้ 28%)** เป็นหลักฐานหลัก — รันจริง 8 รายการ: **nested CV** (ต่าง <1 จุด ✔ ตัวเลขเชื่อถือได้), **selective classification** (BUY 77.7% ที่ coverage 50% ไม่ถึง 80%), **learning curve** (แบนตั้งแต่ n=100 ✔ ชนเพดาน), **pairwise FUEL** (แพ้ baseline ทุกคู่), **hierarchical** (+1.25 จุด), **ordinal** (QWK 0.133), **confident learning** (p=0.46 ไม่มีผล — และพบกับดัก circular evaluation ที่ให้ 86.7% แบบไม่มีความหมาย), **clustering** (Cramér's V 0.04–0.13 label ไม่ตรงโครงสร้างข้อมูล) — **สคริปต์ทั้งหมดเก็บที่ `analysis/` พร้อม README เพื่อทำซ้ำได้** — ผลเต็มใน `agent-docs/รายงานเสนออาจารย์_ทบทวนวรรณกรรม_2026-08-02.md` + one-pager สำหรับยื่นอาจารย์
- [x] **Data (2026-08-02): เทียบชุดข้อมูลเก่า–ใหม่ด้วยวิธีวัดเดียวกัน — พบว่าการเก็บ ICE เพิ่มทำให้ FUEL "แพ้ baseline"** — ตัวเลข 48.7% ของชุดเก่าที่อ้างกันมาตลอดเป็นค่าจาก **split เดียว** และเป็น**ค่าสูงสุด**ของช่วง 31.2%–48.7% — วัดใหม่ด้วย 20 splits เท่ากันพบว่า **ชุดใหม่ accuracy สูงกว่าทั้งสองโมเดล** (BUY 67.15 vs 64.16, FUEL 43.98 vs 40.75) แต่ **lift ชุดเก่าสูงกว่าทั้งคู่** เพราะ baseline ต่างกัน (FUEL: เก่า 37.28% vs ใหม่ 53.16% จากการเก็บ ICE เพิ่มเป็น 52.8%) — **ข้อสรุปเดิมที่ว่า "FUEL ไม่มีอำนาจทำนายเลย" แรงเกินไป** ที่ถูกคือมีสัญญาณอ่อนจริง (+3.47 จุดบนกลุ่มตัวอย่างที่สมดุลกว่า) แต่ถูกกลบด้วยสัดส่วนคลาส — **สรุปว่าไม่ควรเอาโมเดลจากคนละชุดข้อมูลมาใช้ร่วมกัน** (ไม่มีชุดใดชนะด้านเดียว)
- [x] **ML/Data (2026-08-01 ถึง 08-02): เก็บข้อมูลรอบใหม่ n=500 พร้อมคำถามใหม่ 10 ข้อ + พบต้นตอที่แท้จริงของปัญหา** — ผู้ใช้เก็บแบบสอบถามชุดใหม่ (คำถามเดิม + คำถามใหม่ 10 ข้อต่อท้าย ไม่แก้ของเดิม) n=500, ICE 264/Hybrid 124/EV 112. เพิ่มฟีเจอร์ใหม่ใน `feature_encoding.py`/`train_models.py` (TPB: intention/attitude/subjective_norm/pbc_financial/life events + EV adoption: charging_access/ev_exposure/range_anxiety/tco_awareness/incentive_awareness/nep_score) เทรนใหม่ได้ **BUY 68.5%±4.1% (baseline 58.4%, ROC-AUC 0.715) / FUEL 44.3%±6.1% (baseline 53.2% — ต่ำกว่า baseline)** — **ablation พบว่าคำถามใหม่ไม่ช่วยอย่างมีนัยสำคัญ** (BUY +1.4 จุด ซึ่งเล็กกว่าค่าความผันผวน ±4.1, FUEL −2.7 จุด) — **ข้อค้นพบสำคัญที่สุด: label ของ BUY เป็น construct mismatch** (ดูหัวข้อเฉพาะด้านล่าง) — ทำ careless responder screening + sensitivity analysis เพิ่ม พบว่า attention check ใช้ไม่ได้แต่ผลไม่เปลี่ยน (ยืนยันว่าตัวเลขไม่ได้เกิดจากข้อมูลขยะ) — ตรวจสอบข้ามกับ ChatGPT + Claude.ai + NotebookLM ทั้ง 3 ฝั่งสรุปตรงกันว่า **80% ทำไม่ได้ด้วยข้อมูลชุดนี้ ควรหยุดไล่ตัวเลข** — ดูรายละเอียดเต็มใน `agent-docs/รายงานผลคำถามใหม่_2026-08-01.md`
- [x] ML (2026-07-30): อาจารย์ที่ปรึกษาตั้งเป้า >80% held-out test accuracy — ทดลอง RF/GB/XGBoost/soft-voting ensemble, ordinal encoding, 2 กลยุทธ์ feature set (selected/all), composite features ใหม่ (`income_budget_gap`, `mileage_intensity`, `tech_cost_balance`) — **ไปได้ถึง BUY 63.6% / FUEL 45.0% เท่านั้น** ยืนยันด้วยหลักฐาน 5 อัลกอริทึมที่ต่างกันว่าเพดานอยู่ที่สัญญาณข้อมูล ไม่ใช่โมเดล — เพิ่ม dependency `xgboost`, backup เวอร์ชันเดิมไว้ที่ `car-dss/_backup_2026-07-29/`, 41 tests ผ่านหมด ดูรายละเอียดใน `01-architecture.md`

