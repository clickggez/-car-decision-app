# CarDSS — คู่มือสำหรับ AI Agent

ระบบสนับสนุนการตัดสินใจซื้อรถยนต์ (Flask + scikit-learn) เป็นโปรเจกต์ปริญญานิพนธ์
ที่มี AI agent หลายตัวทำงานร่วมกัน

---

## 🛑 ก่อนทำอะไรทั้งสิ้น

**อ่าน [`agent-docs/00-READ-FIRST.md`](agent-docs/00-READ-FIRST.md) ให้จบก่อน**

ไฟล์นั้นรวมข้อเท็จจริงที่ตรวจสอบแล้ว ข้อห้ามเด็ดขาด และข้อผิดพลาดที่เคยเกิดขึ้นจริง
มีแผนงานที่เคยถูกเสนอโดยอ้างข้อเท็จจริงผิดมาแล้ว (เช่น อ้างว่าคลาสสมดุลดีขึ้น
ทั้งที่ความจริงไม่สมดุลกว่าเดิม, อ้างว่ายังไม่ได้ลอง Stacking ทั้งที่มีในโค้ดแล้ว)

จากนั้นอ่านตามลำดับ: `01-architecture.md` → `02-design-system.md` →
`03-task-board.md` → `04-handoff.md`

**ถ้าจะแก้ไฟล์** อ่านหัวข้อ "การแบ่งงานระหว่าง AI หลายตัว" ท้ายไฟล์ `.cursorrules`
ก่อนด้วย — บอกว่าไฟล์ไหนอยู่โซนไหน แตะได้เองหรือต้องขออนุญาตผู้ใช้ก่อน

---

## บริบทสำคัญที่สุด

นี่คือ **งานวิจัยระดับปริญญานิพนธ์ ไม่ใช่การแข่งขัน Kaggle**

ตัวเลขทุกตัวต้องอธิบายและทำซ้ำได้ในการสอบป้องกัน — **ความถูกต้องเชิงระเบียบวิธี
สำคัญกว่าตัวเลขที่สูง** หากต้องเลือกระหว่าง accuracy ที่สูงกว่ากับวิธีที่อธิบายได้
**ให้เลือกวิธีที่อธิบายได้เสมอ**

สิ่งที่ถือว่าไม่ยอมรับ (เคยเกิดขึ้นจริงและถูกจับได้):
- ไล่หา `random_state` หรือ threshold ที่ให้ตัวเลขสูงสุด
- คัดตัวอย่างออกโดยใช้ความผิดพลาดของโมเดลเป็นเกณฑ์ (เคยได้ 86.7% ปลอมจากวิธีนี้)
- เปลี่ยนตัวแปรตามหลังเห็นผล (outcome switching)
- รายงานตัวเลขจาก train/test split เดียว (ต้องใช้ 20 random splits)
- เทียบ accuracy ข้ามชุดข้อมูลที่สัดส่วนคลาสต่างกัน (ต้องเทียบที่ lift)

---

## โครงสร้างโปรเจกต์

```
car-decision-app/
├── AGENTS.md                 ← ไฟล์นี้
├── .cursorrules / .windsurfrules
├── agent-docs/               ← เอกสารประสานงานระหว่าง agent
│   ├── 00-READ-FIRST.md      ← 🛑 อ่านก่อนเสมอ
│   ├── 01-architecture.md    ← สถาปัตยกรรม + Known Issues
│   ├── 02-design-system.md   ← กฎ UI
│   ├── 03-task-board.md      ← งานค้าง / งานที่เสร็จแล้ว
│   ├── 04-handoff.md         ← บันทึกส่งมอบ (ใหม่สุดอยู่บนสุด)
│   └── รายงาน*.md            ← รายงานสำหรับอาจารย์ที่ปรึกษา
├── analysis/                 ← สคริปต์วิเคราะห์ (ทำซ้ำผลได้) + README
├── car-dss/                  ← ตัวแอป Flask
│   ├── app.py
│   ├── config.py             ← USE_MOCK flag
│   ├── train_models.py       ← ⚠️ ห้ามรันโดยไม่ถามผู้ใช้ (ทับ .pkl ที่ deploy อยู่)
│   ├── models/
│   │   ├── predictor.py      ← predict_buy() / predict_fuel()
│   │   ├── feature_encoding.py ← ใช้ร่วมกันทั้ง train และ inference
│   │   └── *.pkl             ← โมเดลที่เว็บใช้จริง
│   ├── templates/ static/ tests/
│   ├── _backup_2026-08-01_pre_newfeatures/   ← แบ็คอัพ
│   └── _models_2026-08-01_newfeatures/       ← โมเดลใหม่ (ยังไม่ deploy)
└── files/
    ├── user_from/            ← ⚠️ ต้องมี CSV ไฟล์เดียวเสมอ
    └── user_from_archive/    ← ข้อมูลเก่า (ไม่มีคำถามใหม่ 10 ข้อ)
```

---

## คำสั่งที่ใช้บ่อย

```bash
# รันเว็บ
cd car-dss && python app.py            # http://127.0.0.1:5000

# รันเทสต์ (ต้องผ่านครบ 41 ตัวเสมอก่อนปิดงาน)
python -m unittest discover -s car-dss/tests

# วิเคราะห์ (ปลอดภัย ไม่แตะ .pkl)
cd analysis && python <ชื่อสคริปต์>.py
```

⚠️ บน Windows ให้ตั้ง `PYTHONIOENCODING=utf-8` ก่อนรัน ไม่งั้นข้อความภาษาไทยจะเพี้ยน

---

## เมื่อทำงานเสร็จ ต้องทำครบ 3 ข้อ

1. รันเทสต์ให้ผ่านครบ 41 ตัว
2. อัปเดต `agent-docs/03-task-board.md` (ย้ายงานที่เสร็จ + เพิ่มงานใหม่ที่ค้าง)
3. เขียนบันทึกไว้ **บนสุด** ของ `agent-docs/04-handoff.md`
   และเพิ่ม Known Issues ใน `01-architecture.md` ถ้าเจอปัญหาใหม่

หากค้นพบข้อเท็จจริงที่ขัดกับที่เคยเข้าใจ — **อัปเดต `00-READ-FIRST.md` ด้วย**

---

## ข้อควรระวังเฉพาะโปรเจกต์นี้

- ผู้ใช้สื่อสารเป็น **ภาษาไทย** และต้องการคำตอบสั้นกระชับ
- **ถามยืนยันก่อนลงมือทำเสมอ** ไม่ว่าจะเขียนโค้ด แก้ไฟล์ หรือ deploy
- ห้ามเพิ่ม dependency โดยไม่อัปเดต `requirements.txt` และแจ้งใน handoff
- ห้ามเปลี่ยน signature ของ `predict_buy()` / `predict_fuel()` โดยไม่ขออนุมัติ
- ห้ามเปลี่ยน schema ของไฟล์ข้อมูล
