# ⛔ อ่านไฟล์นี้ก่อนเสนอแผนหรือแตะโค้ดใด ๆ

> **สำหรับ AI agent ทุกตัวที่ทำงานโปรเจกต์นี้** (Claude Code, Antigravity, Cursor, ฯลฯ)
> ไฟล์นี้รวมข้อเท็จจริงที่ตรวจสอบแล้วและข้อผิดพลาดที่เคยเกิดขึ้นจริง
> **อัปเดตล่าสุด: 28 สิงหาคม 2569**

---

## 1. สถานะปัจจุบันของโปรเจกต์

**งานปรับปรุงความแม่นยำของโมเดลปิดแล้ว** — ไม่ใช่เพราะยอมแพ้ แต่เพราะมีหลักฐานเชิงประจักษ์
12 ชิ้นที่สอดคล้องกันว่าชนเพดานสารสนเทศของข้อมูลแล้ว (ดู `รายงานเสนออาจารย์_ทบทวนวรรณกรรม_2026-08-02.md`)

**ตัวเลขที่ใช้อ้างอิงได้ — ชุดปัจจุบัน (2026-08-09, meta-classifier ตามคำสั่งอาจารย์)**
n=500, 20 random splits, กลุ่มสมดุลสุ่มลด 10 seed แล้วเฉลี่ย
ผลิตโดย `analysis/balanced_subsample.py` → `balanced_results_bagging_2026-08-09.txt`

| โมเดล | กลุ่มตัวอย่าง | Accuracy | Baseline | Lift |
|---|---|---|---|---|
| BUY | เต็ม (500) | 0.6925 ± 0.0391 | 0.5840 | +0.1085 |
| BUY | สมดุล (416) | 0.6666 ± **0.0110** | 0.5000 | **+0.1666** |
| FUEL | เต็ม (316) | 0.4336 ± 0.0570 | 0.5316 | −0.0980 |
| FUEL | สมดุล (204) | 0.4035 ± 0.0410 | 0.3333 | **+0.0702** |

⚠️ **ต้องใช้ทั้ง 4 บรรทัดจากชุดนี้เท่านั้น ห้ามหยิบบรรทัดจากชุดเก่ามาผสม**
(เลือกรายบรรทัดตามว่าอันไหนสวย = outcome switching ที่กรรมการจับได้ง่ายที่สุด)

**ชุดเก่า (2026-08-02, โมเดลเดี่ยว) — เก็บไว้เทียบเท่านั้น**

| โมเดล | กลุ่มตัวอย่าง | Accuracy | Baseline | Lift |
|---|---|---|---|---|
| BUY | เต็ม (500) | 0.6850 | 0.5840 | +0.1010 |
| BUY | สมดุล (416) | 0.6642 ± 0.0313 | 0.5000 | +0.1642 |
| FUEL | เต็ม (316) | 0.4133 | 0.5316 | −0.1183 |
| FUEL | สมดุล (204) | 0.4423 ± 0.0313 | 0.3333 | +0.1090 |

ชุดใหม่ดีกว่า 3 ใน 4 (BUY ทั้งสองกลุ่ม + FUEL เต็ม) แพ้ที่ **FUEL สมดุล −3.9 จุด**
สาเหตุ: บนกลุ่มสมดุล CV เลือก STACK ไป 3/10 รอบ ซึ่งเป็น meta ที่อ่อนที่สุด
(ผู้ใช้ตัดสินใจ 2026-08-09 ว่า **ไม่ตัด STACK ออก** ให้ CV เลือกเองตามรอบ)

**ผลตรวจสุขภาพโมเดล** (`analysis/model_health_check.py` → `model_health_2026-08-09.txt`)
ใช้ตอบคำถามกรรมการได้โดยตรง — โมเดลที่ถูกเลือกคือ **BAGGING ทั้งสองเป้า**

| | BUY | FUEL |
|---|---|---|
| accuracy (ช่วง) | 0.6925 ± 0.0391 (0.630–0.760) | 0.4336 ± 0.0570 (0.328–0.563) |
| **Cohen kappa** | **+0.3528** (fair) | **+0.1351** (อ่อนแต่ไม่ใช่การเดา) |
| kappa เมื่อสลับ label สุ่ม 20 ครั้ง | −0.0093 ± 0.1172 | −0.0020 ± 0.0653 |
| ห่างจากการเดา | **3.1 เท่าของ std** | **2.1 เท่าของ std** |
| ทายคลาสใหญ่สุดเป็นสัดส่วน | 0.6535 (จริง 0.5840) | **0.3773 (จริง 0.5316)** |

✅ **ไม่เดามั่ว** — permutation test ยืนยัน kappa ของการเดาจริง ≈ 0 ตามทฤษฎี
✅ **ไม่ยุบไปทายคลาสใหญ่** — FUEL ทาย ICE **น้อยกว่า**สัดส่วนจริงด้วยซ้ำ
   recall: EV **0.5071** / Hybrid 0.3781 / ICE 0.4294 → จับ EV ได้ดีที่สุด
⚠️ FUEL ยัง**ผันผวนสูง** (ช่วง 23 จุด เทียบ BUY 13 จุด) ห้ามรายงานเลขเดี่ยวไม่มี ±

**Selective classification + Calibration** (`analysis/selective_calibration.py`
→ `selective_calibration_2026-08-09.txt`) — COVERAGE ตรึงที่ [50,70,90]% เท่ารอบเก่า

| coverage | BUY | FUEL |
|---|---|---|
| 100% | 0.6925 | 0.4336 |
| 90% | 0.7078 | 0.4345 |
| 70% | 0.7464 | 0.4433 |
| 50% | **0.7800** | **0.4125** ⚠️ |

| | BUY | FUEL |
|---|---|---|
| ECE | 0.0572 | 0.0632 |
| ความมั่นใจเฉลี่ย vs ความแม่นจริง | 0.6424 vs 0.6925 (**ถ่อมตัว −0.050**) | 0.4180 vs 0.4336 |

✅ **BUY ใช้ selective ได้จริง** 0.6925 → **0.7800** ที่ coverage 50% (ดีกว่ารอบเก่า 0.7770)
   และโมเดล**ถ่อมตัว** ไม่ได้โม้ — ทิศทางที่ปลอดภัยสำหรับระบบแนะนำ

🔴 **FUEL ใช้ selective ไม่ได้ — ที่ coverage 50% accuracy ลดลง (−0.021)**
   ความมั่นใจของ FUEL **ให้ข้อมูลผิดทาง** ไม่ใช่แค่ไร้ข้อมูล
   ช่วงมั่นใจสูงพังชัด: บอก 0.560 ถูกจริง 0.240 (n=25) | บอก 0.630 ถูกจริง 0.000 (n=3)
   *(bin ท้าย n น้อย แต่ selective@50% ใช้ข้อมูลครึ่งชุดเต็มและยืนยันเรื่องเดียวกัน)*

⚠️ **ห้ามเขียนในเล่มว่า "ระบบดัน accuracy ได้ด้วย selective" แบบรวม ๆ — ใช้ได้เฉพาะ BUY**
⚠️ **ห้ามเอา UI แสดง % ความมั่นใจของ FUEL กลับมา** (เอาออกไปแล้ว 2026-07-26 — โชคดี)

**ทางเลือกกรอบปัญหาของ FUEL — วัดครบแล้ว 2026-08-09**
(`analysis/fuel_binary.py`, `analysis/alt_framing.py` → ไฟล์ผล `*_2026-08-09.txt`)

| กรอบปัญหา | accuracy | baseline | lift | kappa | ROC-AUC |
|---|---|---|---|---|---|
| **3 คลาส (ที่ใช้อยู่)** | 0.4336 | 0.5316 | −0.0981 | **+0.1351** | **0.5837** |
| 2 คลาส (EV+Hybrid vs ICE) | **0.5461** | 0.5316 | **+0.0144** | +0.0887 | 0.5758 |

🔑 **ยุบ 2 คลาสทำให้ accuracy พุ่ง +11.25 จุดและชนะ baseline ครั้งแรก
แต่ kappa และ ROC-AUC กลับ *ลดลง* ทั้งคู่** → ตัวเลขที่เพิ่มมาจาก**โจทย์ง่ายลง**
ไม่ใช่โมเดลเก่งขึ้น (lift ขึ้นแค่ +1.4 จุด)
**พิสูจน์ว่าส่วนที่ยากคือการแยก EV ออกจาก Hybrid** ไม่ใช่โมเดลไร้อำนาจทำนาย

➡️ **มติ: คงโมเดลหลักไว้ที่ 3 คลาส** เขียนผล 2 คลาสลงบทที่ 4 เป็นการวิเคราะห์เสริม
เพื่ออธิบายว่าทำไม FUEL ต่ำกว่า baseline — เลือกที่แย่กว่าเชิงตัวเลขเพราะถูกกว่าเชิงวัตถุประสงค์

❌ **top-2 accuracy ใช้ไม่ได้ — เป็นกับดัก**
โมเดลได้ 0.7523 แต่ **baseline ของ top-2 (แนะนำ ICE+Hybrid เสมอ) = 0.7848** → โมเดลแพ้
**ห้ามเขียนว่า "ระบบแนะนำ 2 ตัวเลือกแม่น 75%"**

**เหตุผลที่ FUEL "แพ้ baseline" — เขียนลงบทที่ 4 ได้**
`class_weight="balanced"` + SMOTE ทำให้โมเดลจงใจเสียความแม่นยำรวมเพื่อไม่ทิ้งคลาสน้อย
สังเกตว่า **balanced acc (0.4382) > accuracy (0.4336)** การทาย ICE เสมอจะได้ 53.16% ทันที
แต่ EV recall จะเป็น 0 ซึ่งทำให้ระบบแนะนำรถไร้ประโยชน์ — โมเดลเลือกทางที่ถูกสำหรับ DSS
แล้วถูกตัวชี้วัด accuracy ลงโทษ (accuracy paradox)

**ตรวจสถานะ deploy ล่าสุด 2026-08-28:** โมเดล BAGGING **อยู่บนเว็บแล้ว** ตั้งแต่ 2026-08-09
(`models/buy_model.pkl` 143.8 MB · `models/fuel_model.pkl` 19.2 MB) `verify_deploy.py` ผ่านเกณฑ์ §3.1.1
ครบ 3 ข้อ และ `python -m unittest discover -s tests` ผ่าน 41/41 — **ไม่ต้อง deploy ซ้ำ**
⚠️ เครื่องนี้ไม่มี `pytest` ให้ใช้ `unittest` แทน

**✅ ปิดเบาะแสค้าง 2026-08-28: label FUEL เป็น construct mismatch — ยืนยันแล้ว**
`analysis/fuel_label_construct.py` → `fuel_label_construct_2026-08-28.txt`
label FUEL **ไม่สัมพันธ์กับข้อจำกัดจริงเลยสักตัวจาก 7 ตัว** (p=0.30–0.95) ขณะที่ **BUY สัมพันธ์ 4 ตัวที่ p<0.001**
(จุดชาร์จ/ที่จอด/งบ/ที่พัก) — ข้อมูลและฟีเจอร์ชุดเดียวกัน ต่างกันแค่ label ·
คนเลือก EV ติดจุดชาร์จไม่ได้ 30.4% เทียบคนเลือกน้ำมัน 32.6% (ต่างกัน 2.2 จุด) มีจุดชาร์จอยู่แล้วแค่ 4/112 ·
คนเลือก EV อ้างเหตุผลสิ่งแวดล้อม 4.5% **น้อยกว่า**คนเลือกน้ำมัน 5.3%
→ **หลักฐานชิ้นที่ 17 และเป็นข้อค้นพบเชิงระเบียบวิธีชิ้นที่ 2 ของเล่ม** ใช้ตอบ "ทำไม FUEL ต่ำกว่า baseline" ได้ตรง

**🌐 เว็บ deploy จริงบน PythonAnywhere** — `https://r4tt4.pythonanywhere.com`
อัปเดตด้วย `git push` → เข้า Bash console ของ PythonAnywhere → `git pull` → กด **Reload Web**
(ข้อมูลจาก Antigravity 2026-08-28 · ยืนยันแล้วโดยเปิดเว็บจริง)
⚠️ `firebase-service-account.json` ไม่อยู่ใน git ต้องอัปโหลดแยกผ่านหน้า Files ของ PythonAnywhere

🔴 **เว็บออนไลน์ยังเป็นของเก่าทั้งหมด** (commit `a48be3d`) — โมเดลเก่า 135 KB และ**ไม่มีคำถามใหม่ 10 ข้อ**
งานตั้งแต่ 2026-08-09 ทั้งหมด **ยังไม่เคย commit/push**

**✅ ปิดปัญหาลิมิต GitHub 2026-08-28 ด้วยการบีบอัดโมเดล**
`buy_model.pkl` 137.2 MB → **25.7 MB** · `fuel_model.pkl` 18.4 → **4.6 MB** (`joblib compress=3`)
**ผลทำนายเท่ากันทุกทศนิยม ตัวเลขในเล่มไม่เปลี่ยน** · `verify_deploy.py` ตรงกันทั้ง 4 โปรไฟล์ · tests 41/41
⛔ **ห้าม ignore `car-dss/models/*.pkl`** (เว็บ deploy ด้วย git pull) และ
⛔ **ห้ามเอา `compress=3` ออกจาก `train_models.py`**

**งานที่เหลือ:** ผู้ใช้คุยกับอาจารย์ → ร่างบทที่ 4-5

---

## 2. ❌ ข้อเท็จจริงที่มักเข้าใจผิด — ตรวจสอบแล้วทั้งหมด

### 2.1 "ชุดข้อมูลใหม่มีคลาสสมดุลดีขึ้น" — **ผิด กลับด้าน**

ICE = 264/500 = **52.8%** คือ**ไม่สมดุลกว่าเดิม** ชุดเก่า baseline อยู่ที่ 37.28%
ชุดใหม่พุ่งเป็น **53.16%** → นี่คือ**สาเหตุ**ที่ FUEL แพ้ baseline ไม่ใช่ข้อดี

### 2.2 "ยังไม่ได้ลอง Stacking / XGBoost / Mutual Information" — **ผิด ทำแล้วทั้งหมด**

| อ้างว่ายังไม่ทำ | ความจริง |
|---|---|
| StackingClassifier + LogisticRegression | มีอยู่แล้วที่ `train_models.py:671` ทดสอบแล้วแย่กว่าตัวเดี่ยว |
| XGBoost / HistGradientBoosting ปรับจูน | ทำแล้ว ผ่าน GridSearchCV |
| Mutual Information | ทำแล้ว + permutation test |
| Composite features | ทำแล้ว 5 ตัว ablation บอกว่าไม่ช่วย |
| SMOTE | ทำแล้ว |
| Soft-voting ensemble | ทำแล้ว |
| SVM + ANN ผสมกัน | ทำแล้ว 3 รูปแบบ **แย่ลงทุกแบบ** |

**ยังไม่เคยลองจริงมีแค่ 3 อย่าง:** `ExtraTreesClassifier`, `BorderlineSMOTE`/`SMOTETomek`,
tree-based feature importance สำหรับคัดฟีเจอร์ — ทำได้ แต่ให้ทำเป็นการทดลองใน `analysis/`
**ไม่ใช่แก้ `train_models.py`** และคาดหวังผลไม่เกิน 1-2 จุด

### 2.3 "เพิ่มข้อมูลเป็น 1,000 แล้วจะดีขึ้น" — **ผิด มีหลักฐานว่าไม่ช่วย**

Learning curve ที่รันจริง: n=100 → 67.7%, n=500 → 68.2% **แบนสนิท**
ประวัติจริง: 225 → 505 → 715 → 500 ตัวเลขไม่เคยขยับเกินค่าความผันผวน

---

## 3. 🚫 ข้อห้ามเด็ดขาด (เคยพลาดมาแล้วจริง)

### 3.1 ห้ามคัดตัวอย่างออกโดยใช้ความผิดพลาดของโมเดลเป็นเกณฑ์

**เคยได้ 86.7% ด้วยวิธีนี้ และมันเป็นตัวเลขปลอม**

Confident Learning ที่คัดตัวอย่างต้องสงสัยออกจาก *ทั้งชุด* แล้ววัดใหม่ → 68.5% เป็น 86.7%
แต่เมื่อดูเลข: คัดออก 96 คน แต่ **คนที่ทายผิดหายไป ~104 คน** = คนที่ถูกคัดคือคนที่โมเดลทายผิดแทบทั้งหมด
เท่ากับ **ตัดข้อที่ทำไม่ได้ออกจากข้อสอบแล้วค่อยคิดคะแนน**

ทำถูกวิธี (คัดเฉพาะใน train วัดบน test ที่ไม่แตะ) → **68.05% เทียบ 68.50%, p=0.46 ไม่มีผล**

✅ การคัดที่ทำได้: ใช้**เกณฑ์อิสระจากโมเดล** เช่น attention check / straight-lining
(ทำแล้ว คัด 278 คน ผลไม่เปลี่ยน)

### 3.1.1 ⚠️ unit tests ผ่าน ≠ deploy ได้ (บทเรียนจริง 2026-08-02)

Round 2 ของ Antigravity รัน tests ผ่าน 41/41 แล้ว deploy โมเดลใหม่ → **เว็บแนะนำผิดทันที**
(ผู้ใช้ที่ขับ EV อยู่ ได้รับคำแนะนำให้ซื้อ ICE ด้วยความมั่นใจ 98%)

เพราะ tests ตรวจแค่ **API contract** (คืนค่าครบ field, ไม่ crash)
**ไม่ได้ตรวจว่าคำแนะนำสมเหตุสมผล**

✅ **ก่อน deploy ทุกครั้ง ต้องรัน inference จริงผ่าน `predictor.py` หลายโปรไฟล์
แล้วเทียบกับโมเดลเดิม** — ดูตัวอย่างใน `รีวิว_ML-Optimization-Round2_2026-08-02.md` §1

### 3.2 ห้ามรัน `train_models.py` โดยไม่ถามผู้ใช้

จะทับ `models/buy_model.pkl` และ `fuel_model.pkl` ที่เว็บใช้อยู่ทันที
**`.pkl` ปัจจุบันคือชุดเก่า (n=715) ที่คืนค่าไว้โดยตั้งใจ**

### 3.3 ~~ห้าม deploy โมเดลใหม่ก่อนแก้ฟอร์มเว็บ~~ — ✅ **แก้ฟอร์มและ deploy แล้ว 2026-08-09**

เดิม: โมเดลใหม่ใช้ฟีเจอร์จากคำถามใหม่ 10 ข้อ ซึ่งฟอร์มเว็บไม่มี → ระบบเติมค่า default

**สถานะปัจจุบัน: เพิ่มคำถามครบแล้ว และ deploy โมเดล BAGGING ขึ้นเว็บแล้ว**

- `predict_buy.html` — เพิ่มส่วนที่ 3: `intention` / `attitude` / `subjective_norm` /
  `pbc_financial` (Likert 1-7), `charging_access` / `tco_awareness` /
  `incentive_awareness` (select), `life_events` (checkbox ไม่บังคับ)
- `predict_fuel.html` — เพิ่มส่วนที่ 3: `ev_exposure`, `range_anxiety` (1-7), NEP 5 ข้อ (1-5)
- **3 คำถามกลุ่ม EV ถามที่หน้า buy ครั้งเดียว** แล้วส่งต่อผ่าน `session['buy_input_data']`
  ไปหน้า fuel — ผู้ใช้ไม่ต้องตอบซ้ำ (ดู `app.py.api_predict_fuel`)
- `nep_score` คำนวณใน `app.py` จาก `nep_1..nep_5` โดยกลับคะแนนข้อ 5
  **ตรรกะเดียวกับ `train_models._nep_score` — แก้ที่ใดที่หนึ่งต้องแก้ทั้งคู่**
- `validators.py` เพิ่ม whitelist ครบทุกฟิลด์ (token ต้องตรงกับ dict ใน `train_models.py` เป๊ะ)

**ตรวจก่อน deploy ตาม §3.1.1 แล้ว** (`analysis/verify_deploy.py` → `verify_deploy_2026-08-09.txt`)
เคสที่เคยพัง (คนขับ EV มีที่ชาร์จ ทางไกล): เดิมได้ **ICE 36%** → ใหม่ได้ **EV 45%** ✅
โปรไฟล์อื่นก็สมเหตุสมผลขึ้น (คอนโดไม่มีที่ชาร์จ → ICE 44%, ครอบครัวบ้านเดี่ยว → Hybrid 53%)

⚠️ **แบ็คอัพโมเดลเดิมอยู่ที่ `car-dss/_backup_2026-08-09_pre_bagging/`** — ย้อนได้ทันที
⚠️ ขนาดไฟล์และความเร็วมีปัญหาที่ต้องรู้ — ดู Known Issues ใน `01-architecture.md`

### 3.4 ห้ามเปลี่ยนตัวแปรตาม (label) เป็นคำถาม "เจตนาซื้อ" (คอลัมน์ 44)

เหตุผล: (ก) **outcome switching** — เปลี่ยนหลังเห็นว่าผลเดิมไม่สวย
(ข) **construct leakage** — ฟีเจอร์ `attitude`/`subjective_norm`/`pbc_financial`
ตามทฤษฎี TPB ออกแบบมาอธิบาย intention อยู่แล้ว accuracy จะสูงขึ้นแบบไม่มีความหมาย

### 3.5 ห้ามใช้เกณฑ์ "รันจนกว่าตัวเลขจะขึ้น"

ทุกการทดลองต้อง **ประกาศล่วงหน้าว่าจะรายงานผลไม่ว่าดีขึ้นหรือแย่ลง**
โดยเฉพาะฟีเจอร์ที่เป็นผลรวมถ่วงน้ำหนัก (เช่น `ev_readiness_index`) ซึ่งปรับน้ำหนักจนตัวเลขสวยได้ไม่จำกัด

---

## 4. 📏 กฎการวัดผล (สำคัญมาก — เคยพลาดจนเกือบเขียนลงเล่ม)

1. **ต้องวัดด้วย 20 random splits เสมอ** ห้ามรายงานตัวเลขจาก split เดียว
   *(ตัวเลข 48.7% ของ FUEL ที่อ้างกันมาหลายสัปดาห์ เป็นค่าจาก split เดียว
   และเป็น **ค่าสูงสุด** ของช่วง 31.2%–48.7% — โชคดีที่จับได้ก่อนส่ง)*

2. **ห้ามเทียบ accuracy ข้ามชุดข้อมูลที่สัดส่วนคลาสต่างกัน — ต้องเทียบที่ lift**
   *(ตัวอย่าง: FUEL ชุดเก่า 40.75% ดูแย่กว่าชุดใหม่ 43.98% แต่ lift เก่า +3.5 ใหม่ −9.2)*

3. **`report_baseline_lift.py` ใช้ split เดียว** — ใช้ดูคร่าว ๆ ได้ แต่**ห้ามใช้ตัวเลขนั้นรายงาน**

4. **ถ้ารายงานผลกลุ่มสมดุล ต้องรายงานกลุ่มเต็มควบคู่เสมอ** เพราะตัดสินใจหลังเห็นผลแล้ว

---

## 5. 🔬 ข้อค้นพบเชิงระเบียบวิธีที่ต้องเขียนลงเล่ม

1. **Construct mismatch ของ label BUY** — คำถามที่ใช้เป็น label คือ *"ท่านมีแนวโน้มจะซื้อ
   รถยนต์**ตามประเภทเชื้อเพลิงที่ท่านสนใจ**หรือไม่"* ซึ่งวัด *conditional purchase*
   ไม่ใช่ *purchase intention* → อธิบายได้ว่าทำไมตัวแปรเจตนาซื้อไม่สัมพันธ์กับ label (p=0.207)
   และทำไมจุดชาร์จสัมพันธ์กับ BUY (p=0.0001) แต่ไม่สัมพันธ์กับ FUEL (p=0.950)

2. **สัดส่วนคลาสกลบผลของ FUEL** — บนกลุ่มสมดุล lift เปลี่ยนจาก −11.8 เป็น **+10.9 จุด**

3. **ข้อบกพร่องแบบสอบถาม** — โน้ต `[ข้อนี้เป็น reverse-worded...]` หลุดไปให้ผู้ตอบเห็น
   ทำให้ attention check ใช้ไม่ได้ (correlation +0.049 ทั้งที่ควรเป็นลบ)

4. **กับดัก circular evaluation ของ Confident Learning** (ดูข้อ 3.1)
   → เขียนเป็นข้อสังเกตเชิงระเบียบวิธีได้ แสดงว่ารู้ทันกับดัก

5. **Intention–Behavior Gap** (Sheeran 2002, meta-analysis 422 การศึกษา: เจตนาอธิบาย
   พฤติกรรมจริงได้ 28%) = กรอบทฤษฎีที่อธิบายเพดาน

---

## 6. 📂 แผนผังไฟล์

| ต้องการ | ไปที่ |
|---|---|
| บริบทล่าสุด + ข้อควรระวัง | `04-handoff.md` (บนสุด) |
| งานค้าง | `03-task-board.md` |
| Known Issues + สถาปัตยกรรม | `01-architecture.md` |
| **รายงานหลักสำหรับอาจารย์** | `รายงานเสนออาจารย์_ทบทวนวรรณกรรม_2026-08-02.md` |
| สคริปต์วิเคราะห์ (ทำซ้ำได้) | `../analysis/` + `README.md` ในนั้น |
| โมเดลใหม่ (ยังไม่ deploy) | `../car-dss/_models_2026-08-01_newfeatures/` |
| แบ็คอัพก่อนแก้ | `../car-dss/_backup_2026-08-01_pre_newfeatures/` |
| ข้อมูลเก่า (ไม่มีคำถามใหม่) | `../files/user_from_archive/` |

⚠️ **`files/user_from/` ต้องมีไฟล์ CSV เดียวเสมอ** เพราะ `load_survey()` อ่านแค่ `matches[0]`

---

## 7. ✍️ ก่อนเสนอแผนใด ๆ ให้ตอบคำถามเหล่านี้ก่อน

1. สิ่งที่จะเสนอ อยู่ในตาราง §2.2 (ทำไปแล้ว) หรือไม่
2. จะวัดผลด้วย 20 splits หรือ split เดียว
3. ถ้าผลออกมาแย่ลง จะรายงานหรือไม่ (ต้องตอบว่า "รายงาน")
4. จะแตะ `.pkl` ที่ deploy อยู่หรือไม่ (ต้องตอบว่า "ไม่")
5. เกณฑ์ตัดสินว่าสำเร็จคืออะไร — ถ้าตอบว่า "accuracy เพิ่มขึ้น" **แสดงว่าแผนมีปัญหา**
   เพราะเป็นการเลือกตามผล ให้เปลี่ยนเป็น "รายงานผลตามจริงเพื่อปิดช่องว่างในการตรวจสอบ"
